# packaging

Packaging work is intentionally deferred.

The long-term direction is a desktop-distributed artifact that can eventually be shipped as a Windows executable and bundled through an installer. This repository does not include build pipelines, installer configs, update systems, or release automation.
