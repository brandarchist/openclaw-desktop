# bootstrap

Startup and runtime initialization concepts belong here. In a fuller implementation this would assemble configuration, preflight checks, and service wiring before the desktop shell marks the runtime as available.
