# Local Gateway

The intended direction is a local gateway that the desktop shell can call into without pretending the UI itself is the runtime.

This repository does not implement that gateway yet. The point of this placeholder is to keep the seam visible from the start.
