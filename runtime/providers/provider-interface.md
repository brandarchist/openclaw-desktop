# Provider Interface

This directory reserves the provider/model seam only.

Future implementations are expected to normalize:

- request and response shapes
- model capability discovery
- runtime availability checks
- local versus remote provider differences
