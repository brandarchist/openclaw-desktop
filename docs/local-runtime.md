# Local Runtime

The local runtime is expected to be distinct from the desktop shell even when both ship together. The shell presents UI and inspection surfaces. The runtime owns execution, readiness, and later OpenClaw orchestration.
