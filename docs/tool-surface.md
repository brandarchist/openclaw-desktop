# Tool Surface

The eventual tool surface should favor explicit attachment and user-visible behavior over hidden automation. That implies clear runtime state, visible execution boundaries, and inspectable interactions with the local workspace.
